<aside class="mock-banner" role="status">
  <span class="mock-banner__icon" aria-hidden="true">⚙️</span>
  <span class="mock-banner__text">
    실데이터 연결 안 됨 — 지금은 임시 카드를 보여드리고 있어요
  </span>
</aside>

<style>
  .mock-banner {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    margin: 0 var(--space-md) var(--space-sm);
    padding: var(--space-sm) var(--space-md);
    background: #fff8e1;
    border: 1px solid #ffe082;
    border-radius: var(--radius-md);
    font-size: var(--font-size-sm);
    color: #6b5d2c;
  }

  .mock-banner__icon {
    font-size: 16px;
    flex-shrink: 0;
  }

  .mock-banner__text {
    flex: 1;
    line-height: 1.4;
  }
</style>
